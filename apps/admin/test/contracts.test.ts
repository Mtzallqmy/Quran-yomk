import test from 'node:test';import assert from 'node:assert/strict';import {normalizeArabic,publicExternalEligible,publicInternalEligible,assertUuid,ApiError} from '../lib/contracts.ts';
test('Arabic normalization handles alef, diacritics and tatweel',()=>{assert.equal(normalizeArabic('إِقْــرَأ آياتِ الهُدى'),'اقرا ايات الهدي')});
test('external production eligibility is deny-by-default and requires explicit release approval',()=>{assert.equal(publicExternalEligible({station_source:'EXTERNAL',is_active:true,production_enabled:false,availability_status:'PLAYABLE_IN_DEVELOPMENT',rights_status:'APPROVED',commercial_use_status:'ALLOWED',health_status:'HEALTHY',deleted_at:null},'production'),false);assert.equal(publicExternalEligible({station_source:'EXTERNAL',is_active:true,production_enabled:true,availability_status:'APPROVED_FOR_PUBLIC_RELEASE',rights_status:'APPROVED',commercial_use_status:'ALLOWED',health_status:'HEALTHY',deleted_at:null},'production'),true)});
test('external development eligibility depends on technical health without fabricating release approval',()=>{assert.equal(publicExternalEligible({station_source:'EXTERNAL',is_active:true,production_enabled:false,availability_status:'PLAYABLE_IN_DEVELOPMENT',rights_status:'REVIEW_REQUIRED',commercial_use_status:'UNKNOWN',health_status:'HEALTHY',deleted_at:null},'development'),true);assert.equal(publicExternalEligible({station_source:'EXTERNAL',is_active:true,production_enabled:false,availability_status:'PLAYABLE_IN_DEVELOPMENT',health_status:'UNREACHABLE',deleted_at:null},'development'),false)});
test('internal development station can be visible without production flag',()=>{assert.equal(publicInternalEligible({station_source:'INTERNAL',is_active:true,production_enabled:false,deleted_at:null},'development'),true);assert.equal(publicInternalEligible({station_source:'INTERNAL',is_active:true,production_enabled:false,deleted_at:null},'production'),false)});
test('UUID validation rejects malformed IDs',()=>{assert.throws(()=>assertUuid('../etc/passwd'),ApiError)});
